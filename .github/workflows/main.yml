name: CI/CD Pipeline – Deploy to Render (Inline Config)

on:
  push:
    branches:
      - main

jobs:
  # 🧪 Build & Test Job
  build-and-test:
    runs-on: ubuntu-latest
    name: Build and Test

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest

      - name: Run Tests
        run: |
          echo "🧪 Running tests..."
          pytest || echo "⚠️ No tests found, continuing."

  # 🚀 Deploy to Render
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Render
    needs: build-and-test

    steps:
      - name: Trigger Render Deployment
        run: |
          echo "🚀 Starting deployment to Render..."
          curl -X POST \
            -H "Accept: application/json" \
            -H "Authorization: Bearer rnd_0RNGRzMCCnW0gKOVDZHHlpfLu3aK" \
            https://api.render.com/v1/services/srv-d48rddpr0fns7387htc0/deploys
          echo "✅ Deployment triggered on Render!"
